name: iOS Testing Simple

on:
  push:
    branches: [main, develop]
    paths:
      - 'ios/**'
      - '.github/workflows/ios-tests-simple.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'ios/**'

env:
  XCODE_VERSION: '16.1'

jobs:
  test:
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        test-type: ['unit', 'ui']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Cache Xcode Derived Data
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
            ios/build
          key: xcode-${{ runner.os }}-${{ env.XCODE_VERSION }}-simple-${{ hashFiles('ios/**/*.swift', 'ios/**/*.plist', 'ios/**/*.xcconfig') }}

      - name: Build for Testing
        run: |
          echo "Building for iOS testing with debugging..."

          # Debug available iOS versions and SDKs
          echo "Available iOS Simulator SDKs:"
          xcrun xcodebuild -showsdks | grep simulator || true

          echo "Available iOS Simulators:"
          xcrun simctl list runtimes | grep iOS || true

          echo "Available device types:"
          xcrun simctl list devicetypes | grep iPhone || true

          echo "Available simulator devices:"
          xcrun simctl list devices available | grep iPhone || true

          # Create a simulator device with available runtime if needed
          echo "Creating iPhone 15 Pro with iOS 18.4 runtime..."
          DEVICE_ID=$(xcrun simctl create "iPhone 15 Pro iOS 18.4" "iPhone 15 Pro" "com.apple.CoreSimulator.SimRuntime.iOS-18-4")
          echo "Created simulator device: $DEVICE_ID"

          # Use the newly created device
          DESTINATION="platform=iOS Simulator,id=$DEVICE_ID"
          echo "Using created device destination: $DESTINATION"

          # Build for testing with more flexible settings
          xcodebuild build-for-testing \
            -project ios/HealthKitBridge.xcodeproj \
            -scheme HealthKitBridge \
            -destination "$DESTINATION" \
            -derivedDataPath ios/build \
            -configuration Debug \
            -sdk iphonesimulator \
            -enableCodeCoverage YES \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ENABLE_BITCODE=NO \
            ONLY_ACTIVE_ARCH=YES \
            SKIP_INSTALL=YES

      - name: Run Tests
        run: |
          echo "Running tests with created simulator device..."

          # Find the created device
          DEVICE_ID=$(xcrun simctl list devices | grep "iPhone 15 Pro iOS 18.4" | head -1 | sed -n 's/.*(\([^)]*\)).*/\1/p')
          DESTINATION="platform=iOS Simulator,id=$DEVICE_ID"
          echo "Using device ID destination: $DESTINATION"

          if [ "${{ matrix.test-type }}" == "unit" ]; then
            TEST_SCHEME="HealthKitBridgeTests"
          else
            TEST_SCHEME="HealthKitBridgeUITests"
          fi

          xcodebuild test-without-building \
            -project ios/HealthKitBridge.xcodeproj \
            -scheme HealthKitBridge \
            -destination "$DESTINATION" \
            -derivedDataPath ios/build \
            -sdk iphonesimulator \
            -enableCodeCoverage YES \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
