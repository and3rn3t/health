import SwiftUI

// Stubbed AppShell to unblock test build; original implementation disabled.
struct AppShell: View {
    var body: some View {
        Text("AppShell stub")
    }
}

#Preview { AppShell() }
