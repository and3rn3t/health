# AGENTS.md — VitalSense (health)

**Read `CLAUDE.md` in this repo — it is the established source of truth for stack, structure, commands, and conventions here.** This pointer exists so non-Claude agents (Copilot, Cursor, etc.) find the same instructions.

Key invariants (duplicated for safety):

- Branding is **VitalSense** in all user-facing text — never "Health App".
- pnpm, Node 24 (`.nvmrc`); conventional commits enforced via commitlint.
- Don't commit or deploy unless explicitly asked.
